import {
	logout,
	authorize,
	register,
	fetchRoles,
	fetchUsers,
	updateUserRole,
	delUser,
} from './operations';

export const server = {
	logout,
	authorize,
	register,
	fetchRoles,
	fetchUsers,
	updateUserRole,
	delUser,
};
